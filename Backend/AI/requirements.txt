# ATHLASX Compute Service — Python dependencies
#
# NOTE: ARM64 (Oracle Ampere A1) is the eventual deployment target. All pinned
# packages below publish manylinux aarch64 wheels (or are pure-Python), so the
# image builds on ARM without source compilation.

# --- Web / ASGI ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Async DB (SQLAlchemy 2.x async + Postgres driver) ---
sqlalchemy[asyncio]==2.0.34
asyncpg==0.29.0
alembic==1.13.2

# --- Background jobs (ARQ) + Redis ---
arq==0.26.1
redis==5.0.8

# --- AI provider ---
# google-genai exposes a native async surface (client.aio.*). Re-verify the
# pinned video PREVIEW model string against https://ai.google.dev/gemini-api/docs/models
# before production — preview models can be deprecated with ~2 weeks' notice.
google-genai>=1.0.0

# --- Config / validation ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Auth (standalone HS256 JWT — swap for Auth.js at integration time) ---
pyjwt==2.9.0

# --- Resilience ---
tenacity==9.0.0

# --- HTTP client (YouTube oEmbed check, OCR image fetch) ---
httpx==0.27.2

# --- Observability (optional; Sentry only initialised if SENTRY_DSN is set) ---
sentry-sdk==2.14.0

# --- Tests ---
pytest==8.3.3
pytest-asyncio==0.24.0
fakeredis==2.24.1
aiosqlite==0.20.0
