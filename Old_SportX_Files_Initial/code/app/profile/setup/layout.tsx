import type { Metadata } from "next";

export const metadata: Metadata = { title: "Profile Setup | AthlasX" };

export default function SetupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
